Ext.data.JsonP.customcontrols({"guide":"<h1 id='customcontrols-section-custom-control'>Custom Control</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/customcontrols-section-why-should-i-care'>Why should I care</a></li>\n<li><a href='#!/guide/customcontrols-section-my-first-control'>My first control</a></li>\n</ol>\n</div>\n\n<p>A guide about custom controls, which are the meat and potatoes of MEPH.</p>\n\n<h2 id='customcontrols-section-why-should-i-care'>Why should I care</h2>\n\n<p>Well, most developers would agree that modularization is key in the development of enterprise level software. Also, because everything in a MEPH application is a custom control, even the views are controls.</p>\n\n<h2 id='customcontrols-section-my-first-control'>My first control</h2>\n\n<p>This will be a unrealistically simple control just for a clear explanation of basic features available when creating a custom control. When we are finished we want the following to be able to write the following :</p>\n\n<pre><code>        &lt;pageheader title=\"Page Title\"  subtitle=\"Sub title\" description=\"A description\"&gt;&lt;/pageheader&gt;\n</code></pre>\n\n<p>That will output this when rendered to the page :</p>\n\n<pre><code>        &lt;h1&gt;Page Title&lt;/h1&gt;\n        &lt;h2&gt;Sub title&lt;/h2&gt;\n        &lt;h3&gt;A description&lt;/h3&gt;\n</code></pre>\n\n<p>Before continuing, a question that could be asked is why creating a custom control like this a good idea. Specifically, if a large application had the same pageheader at the top of every page, and it was decided that the description section should be an <strong>h4</strong> instead of an <strong>h3</strong>. Then a developer would not have to go to every page to change the code, but only edit a single location to change all the locations with little to no diffifculty.</p>\n\n<p>Ok, lets do this.</p>\n\n<ul>\n<li><p><strong>Step 1</strong>: Create the PageHeader.js and PageHeader.html files.</p></li>\n<li><p><strong>Step 2</strong>:  Define the PageHeader class.(PageHeader.js)</p>\n\n<pre><code>     MEPH.define('MEPH.pageheader.PageHeader', {\n         alias: 'pageheader',\n         templates: true,\n         extends: 'MEPH.control.Control',\n         properties : {\n             title: '',\n             subtitle: '',\n             description: ''\n         },\n         intialize: function(){\n             var me = this;\n             me.addAutoBindProperty(['title','subtitle','description']);\n             me.addTransferables(['title', 'subtitle', 'description']);\n             me.callParent.apply(me, arguments);\n         }\n     });\n</code></pre></li>\n<li><p><strong>Step 3</strong>: Define the PageHeader Template (PageHeader.html)</p>\n\n<pre><code>     &lt;h1 data-bind='\"innerHTML\" : \"c$.title\"'&gt;&lt;/h1&gt;\n     &lt;h2 data-bind='\"innerHTML\" : \"c$.subtitle\"'&gt;&lt;/h2&gt;\n     &lt;h3 data-bind='\"innerHTML\" : \"c$.description\"'&gt;&lt;/h3&gt;\n</code></pre></li>\n</ul>\n\n\n<p>That's it.</p>\n\n<h3 id='customcontrols-section-details'>Details</h3>\n\n<p>You may have notice a few properties of the class configuration that haven't been explain, don't worry its about to happen.</p>\n\n<pre><code>            MEPH.define('MEPH.pageheader.PageHeader', {\n                alias: 'pageheader',\n                templates: true,\n                ...\n</code></pre>\n\n<ul>\n<li><strong>alias</strong></li>\n</ul>\n\n\n<p>Alias tells MEPH that when it see a html object with the node name <strong>pageheader</strong>, that it is referencing the MEPH.pageheader.PageHeader. This also means that the alias must be unique throughout the system.</p>\n\n<p>If alias was <strong>candycrushercontrol</strong> , then</p>\n\n<pre><code>            &lt;candycrushercontrol&gt;&lt;/candycrushercontrol&gt;\n</code></pre>\n\n<p>would be handled by MEPH, and would output whatever the template says.</p>\n\n<ul>\n<li><strong>templates</strong></li>\n</ul>\n\n\n<p>In the example <strong>templates</strong> equals <strong>true</strong>, this is a convention of MEPH. That when the templates is set to true, the template is assumed to be located in a file in the same directory as the class with a <strong>.html</strong> extension instead of the <strong>.js</strong> extention. The templates property can be set in multiple ways like :</p>\n\n<p>The most common and easiest.</p>\n\n<pre><code>            ...\n            templates: true\n            ...\n</code></pre>\n\n<p>This is the equivalent of the previous convention.</p>\n\n<pre><code>            ...\n            templates: 'MEPH.pageheader.PageHeader'\n            ...\n</code></pre>\n\n<p>or</p>\n\n<pre><code>            ...\n            templates : ['MEPH.pageheader.PageHeader']\n            ...\n</code></pre>\n\n<p>or</p>\n\n<pre><code>            ...\n            templates : ['MEPH.pageheader.PageHeader', 'MEPH.pageheader.ExtraPart']\n            ...\n</code></pre>\n\n<p>The last example shows a way of setting the template, and additional resources which can be utilized by the control.</p>\n\n<p>In our example control we have attributes set on the pageheader node. The attributes set here are not automatically set to the properties of the control instance. It is required to add them to the list of transferable attributes.</p>\n\n<pre><code>            ...\n            me.addTransferables(['title', 'subtitle', 'description']);\n            ...\n</code></pre>\n\n<p>This will cause the properties title, subtitle and description to pass the attribute values set on the pageheader node to be set to the instance of the obbject.</p>\n\n<p><strong>What if those values need to be bound a controller or model?</strong></p>\n\n<p>That's what this does.</p>\n\n<pre><code>            ...\n            me.addAutoBindProperty(['title','subtitle','description']);\n            ...\n</code></pre>\n\n<p>If those properties should bind their values to a referrable instance then the <strong>addAutoBindProperty</strong> function will do that.</p>\n\n<p>That's all folks.</p>\n","title":"Custom Controls 101"});